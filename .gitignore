# Server runtime data
server/data/
server/backups/
server/geyser/*.jar
server/geyser/logs/
server/geyser/dumps/
server/geyser/packs/
server/geyser/key.pem
server/geyser/public-key.pem
server/geyser/saved-refresh-tokens.json

# Local environment overrides
.env
server/world/
server/world_nether/
server/world_the_end/
server/logs/
server/crash-reports/
server/*.log
server/eula.txt

# Docker volumes
docker/data/

# Node.js
bots/node_modules/
bots/package-lock.json
bots/.env
agents/bot/node_modules/
agents/bot/package-lock.json

# Python
agent-bridge/__pycache__/
agent-bridge/.venv/
agent-bridge/.env
agent-bridge/*.pyc
agents/**/__pycache__/
agents/**/*.pyc
**/__pycache__/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Agent context / planning (local-only)
.lattice/
hermes_conversation_*.json
.hermes/

# Session logs
*.log
sessions/
logs/

# Project memory / planning docs (live in wiki)
MEMORY.md
PROJECT.md
EPIC-*.md
# docs/ is no longer gitignored — design docs are canonical references
# docs/archive/ contains outdated docs kept for historical reference

# LuckPerms runtime DB (not gitignored by the server/data catch-all since
# LP is installed into data/ which is already ignored — this covers the
# manually tracked copy if someone adds it directly)
server/plugins/luckperms/*.db
server/plugins/luckperms/luckperms-h2*

# Built client pack artifacts
dist/

# Generated bot config JSON (from YAML at runtime)
agents/bot/config-*.json

# primitives_lab — timestamped experiment outputs
agents/embodied-service/primitives_lab/results/
agents/data/
